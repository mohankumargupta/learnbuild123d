{
    "tasks": [
        {
            "label": "JupyText Sync",
            "type": "shell",
            "command": "jupytext --sync notebooks/*.py",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "presentation": {
                "reveal": "never",
            }
        }
    ]
}